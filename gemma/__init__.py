"""Gemma — the only part of the system allowed to be non-deterministic.

Gemma classifies and explains. It never decides and never counts. Every number
that reaches a screen or an order is computed in `selection/` or `trading/`.
"""
