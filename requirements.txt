fastapi
uvicorn[standard]
pydantic
python-dotenv
pyyaml
pytest
httpx

# Track 2 — trading
kiteconnect

# Track 1 — sentiment/selection (listed so one venv serves the whole repo)
google-genai          # Gemma 4 via Google AI Studio — the default backend
ollama                # optional local backend, used only if the daemon answers
yfinance
pandas
numpy
pyarrow               # parquet price cache
feedparser
