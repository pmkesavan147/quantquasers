# Runtime — what a deployed instance actually imports.
#
# Kept lean on purpose: Vercel's function size limit is 250 MB unzipped, and
# yfinance/pyarrow/kiteconnect are only needed when *collecting* data, which a
# serverless instance never does (it serves data/snapshot/). For local
# development, install requirements-dev.txt instead — it includes this file.
fastapi
uvicorn[standard]
pydantic
python-dotenv
pyyaml
httpx                 # API client, and the Supabase journal talks over HTTP
pandas                # quant metrics
numpy
google-genai          # Gemma 4 via Google AI Studio — the default backend
