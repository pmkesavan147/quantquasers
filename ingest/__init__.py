"""Data in: prices from yfinance, headlines from Google News RSS.

Both cache to disk and both serve the cache when the network is unavailable.
Venue Wi-Fi is not to be trusted, and a demo that needs the internet to show a
number it already fetched once is a demo that fails for no reason.
"""
